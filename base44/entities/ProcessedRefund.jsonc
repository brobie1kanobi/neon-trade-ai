{
  "name": "ProcessedRefund",
  "type": "object",
  "properties": {
    "session_id": {
      "type": "string",
      "description": "Stripe session ID that was refunded"
    },
    "user_email": {
      "type": "string",
      "description": "Email of the user who received the refund"
    },
    "admin_email": {
      "type": "string",
      "description": "Email of the admin who processed the refund"
    },
    "type": {
      "type": "string",
      "enum": [
        "credits",
        "subscription",
        "deposit"
      ],
      "description": "Type of refund processed"
    },
    "processed_at": {
      "type": "string",
      "format": "date-time",
      "description": "When the refund was processed"
    },
    "result": {
      "type": "string",
      "description": "JSON string of the refund processing result"
    }
  },
  "required": [
    "session_id",
    "user_email",
    "admin_email",
    "type",
    "processed_at"
  ],
  "rls": {
    "create": {
      "user_condition": {
        "role": "admin"
      }
    },
    "read": {
      "$or": [
        {
          "created_by": "{{user.email}}"
        },
        {
          "data.user_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}